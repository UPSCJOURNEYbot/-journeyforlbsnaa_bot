# ---- Telegram ----
kurigram
# TgCrypto is an OPTIONAL C accelerator for kurigram's MTProto AES; without
# it kurigram transparently uses the pure-Python pyaes fallback (pinned
# below, "Pyrogram will work the same, but at a much slower speed").
# TgCrypto 1.2.5 (also its latest release) ships wheels only up to CPython
# 3.11; on Python 3.12 (the Ubuntu 24.04 VPS runtime) an unconditional pin
# forces a source build needing gcc/python3.12-dev (absent from the minimal
# VPS image) and fails the entire pip install. Skip it on >=3.12; the bot
# still runs on pyaes. 3.11/Docker keeps the C accelerator wheel.
tgcrypto==1.2.5; python_version < "3.12"
pyaes==1.6.1
python-telegram-bot==22.8
motor==3.7.1

# ---- Async HTTP / files ----
aiohttp==3.10.10
aiofiles==24.1.0

# ---- Config ----
python-dotenv==1.0.1

# ---- Text / HTML processing ----
beautifulsoup4==4.12.3
unidecode==1.3.8

# ---- PDF generation  ----
# WeasyPrint 62.3 (2024-06-21) declares only LOWER bounds on its pure-Python
# PDF stack (pydyf>=0.10.0, tinycss2>=1.3.0 ...) with NO upper bound, so a
# fresh `pip install -r requirements.txt` after 2025-12-02 silently pulls
# pydyf 0.12.x, whose Stream class removed transform(). WeasyPrint 62.3
# weasyprint/pdf/stream.py calls super().transform() for every coordinate
# transform and then crashes at page serialisation with:
#   AttributeError: 'super' object has no attribute 'transform'
# (seen in production on quizbot.service, Ubuntu 24.04 / Python 3.12).
# Pin the exact dependency set WeasyPrint 62.3 shipped with; every pin below
# has cp312 manylinux / py3-none-any wheels for Ubuntu 24.04 / Python 3.12.
weasyprint==62.3
pydyf==0.10.0
tinycss2==1.3.0
cssselect2==0.7.0
pyphen==0.15.0
fonttools[woff]==4.53.1
cffi==1.16.0
html5lib==1.1
PyMuPDF==1.24.11
Pillow==11.0.0
pytesseract==0.3.13
latex2mathml==3.81.1

# ---- Scheduling ----
APScheduler==3.10.4

# ---- Payments ----
razorpay==1.4.2

# ---- Translation----
deep-translator==1.11.4

# ---- Timezones ----
pytz==2024.2

# ---- Mini App----
fastapi==0.115.5
uvicorn[standard]==0.32.1
cryptography==43.0.3
pydantic>=2.10,<3
google-genai>=1.30.0

# ---- Self-hosted Test Series PDF microservice (pip-only, no system libs) ----
fpdf2==2.8.8
uharfbuzz==0.56.1
